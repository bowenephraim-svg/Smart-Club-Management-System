<%- include('../partials/header') %>
<div class="app-layout-container">
    <%- include('../partials/sidebar') %>

    <div class="main-content-window">
        <%- include('../partials/navbar') %>

        <main class="workspace-viewport">
            <div class="back-navigation-row">
                <a href="/activities" class="back-link"><i class="fas fa-arrow-left"></i> Return to Activities</a>
            </div>

            <% if (typeof success_msg !== 'undefined' && success_msg) { %>
                <div class="alert alert-success"><i class="fas fa-check-circle"></i> <%= success_msg %></div>
            <% } %>
            <% if (typeof error_msg !== 'undefined' && error_msg) { %>
                <div class="alert alert-danger"><i class="fas fa-exclamation-triangle"></i> <%= error_msg %></div>
            <% } %>

            <div class="form-workspace-card">
                <div class="form-card-header">
                    <h3><%= pageTitle || 'Create Activity' %></h3>
                    <p>Capture the activity details, club assignment, and venue information with clear validation.</p>
                </div>

                <form action="<%= action %>" method="POST" class="standard-system-form">
                    <div class="form-grid-two-columns">
                        <div class="input-field-wrapper">
                            <label for="club_id">Club <span class="required">*</span></label>
                            <select id="club_id" name="club_id" required>
                                <option value="" disabled <%= !formData.club_id ? 'selected' : '' %>>Select a club...</option>
                                <% if (clubs && clubs.length > 0) { %>
                                    <% clubs.forEach(club => { %>
                                        <option value="<%= club.club_id %>" <%= String(formData.club_id) === String(club.club_id) ? 'selected' : '' %>><%= club.club_name %></option>
                                    <% }); %>
                                <% } %>
                            </select>
                        </div>

                        <div class="input-field-wrapper">
                            <label for="activity_name">Activity Name <span class="required">*</span></label>
                            <input type="text" id="activity_name" name="activity_name" value="<%= formData.activity_name || '' %>" placeholder="e.g. Annual Hackathon Showcase" required>
                        </div>
                    </div>

                    <div class="form-grid-two-columns">
                        <div class="input-field-wrapper">
                            <label for="activity_date">Activity Date <span class="required">*</span></label>
                            <input type="date" id="activity_date" name="activity_date" value="<%= formData.activity_date || '' %>" required>
                        </div>

                        <div class="input-field-wrapper">
                            <label for="venue">Venue <span class="required">*</span></label>
                            <input type="text" id="venue" name="venue" value="<%= formData.venue || '' %>" placeholder="e.g. Science Complex Lab 3B" required>
                        </div>
                    </div>

                    <div class="input-field-wrapper">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" rows="4" placeholder="Add a short description for the activity..."><%= formData.description || '' %></textarea>
                    </div>

                    <div class="form-action-footer-row">
                        <a href="/activities" class="btn btn-muted">Cancel</a>
                        <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> <%= submitLabel || 'Save Activity' %></button>
                    </div>
                </form>
            </div>
        </main>

        <%- include('../partials/footer') %>
    </div>
</div>